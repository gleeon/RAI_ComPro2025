#include <stdio.h>

int main()
{
    int n, c;
    printf("Enter number of elements: ");
    scanf("%d", &n);
    printf("Enter %d integer(s): ", n);
    int m[n];
    for(c=0; c<n; c++){
        scanf("%d", m[n]);
        for(c=0; c<v; c++){}
    }

        for()
        if(m[])
    }
}